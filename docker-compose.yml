version: "3.9"

services:
  backend:
    image: dastraus007/notes-app-backend:latest
    ports:
      - "5000:5000"

  frontend:
    image: dastraus007/notes-app-frontend:latest
    ports:
      - "80:80"
